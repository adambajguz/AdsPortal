@inherits PropertyRenderer<FileResponse?>

@inject IOptions<ApplicationConfiguration> ApplicationConfiguration

<td>
    @if (Value is not null)
    {
        <img id="i-@Value.Id" src="@($"{ApplicationConfiguration.Value.StaticUrl}media/file/{Value.Path}")" alt="@Value.Alt" title="@Value.Title" />
    }
</td>

