# Infrastructure Layer :: Common (CrossCutting Concerns)

This layer contains classes for cross cutting concerns such as cache.
These classes should be based on interfaces defined within the common layer.
