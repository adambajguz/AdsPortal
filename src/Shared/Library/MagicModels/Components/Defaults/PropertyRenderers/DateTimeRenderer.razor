@inherits PropertyRenderer<DateTime>

@if (IsWrite)
{
    <label>
        @PropertySchema.DisplayName
        <InputDate @bind-Value="Value" />
    </label>
}
else
{
    <p><b>@(PropertySchema.DisplayName):</b> @Value.ToLongDateString() @Value.ToLongTimeString()</p>
}

