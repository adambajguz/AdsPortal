@using MagicModels.Interfaces

@inject IModelRenderService RenderService

@RenderService.RenderModel(Model, Context, IsWrite)

@code {
    [Parameter]
    public object? Model { get; init; }

    [Parameter]
    public object? Context { get; init; }

    [Parameter]
    public bool IsWrite { get; init; }
}