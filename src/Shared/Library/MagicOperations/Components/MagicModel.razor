@using MagicOperations.Interfaces

@inject IMagicRenderService RenderService

@RenderService.RenderModel(Model, IsWrite)

@code {
    [Parameter]
    public object? Model { get; init; }

    [Parameter]
    public bool IsWrite { get; init; }
}