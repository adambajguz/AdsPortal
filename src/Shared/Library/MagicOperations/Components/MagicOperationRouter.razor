@using MagicOperations.Interfaces

@inject IMagicRenderService RenderService

@RenderService.RenderOperationRouter(BasePath, ArgsFallback)

@code {
    [Parameter]
    public string? BasePath { get; init; }

    [Parameter]
    public string? ArgsFallback { get; init; }
}