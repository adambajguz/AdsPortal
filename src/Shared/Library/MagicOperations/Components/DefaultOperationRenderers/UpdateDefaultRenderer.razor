@using Components.OperationRenderers

@inherits UpdateOperationRenderer

<h3>UpdateDefaultRenderer</h3>

@if (Errors is not null)
{
    <p>@Errors.ToString()</p>
}
else
{
    <EditForm Model="Model">
        @foreach (var fragment in RenderFragments)
        {
            @fragment
        }
    </EditForm>
}
