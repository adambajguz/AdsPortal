@using MagicOperations.Interfaces

@inject IMagicRenderService RenderService

@if (Path is not null)
{
    @RenderService.RenderOperation(Path)
}
else
{
    throw new MagicOperations.Extensions.MagicOperationsException($"{nameof(Path)} cannot be set to null");
}

@code
{
    [Parameter]
    public string? Path { get; init; }
}